// Online C compiler to run C program online
#include <stdio.h>
struct student{
    char name[10];
    int roll;
};
int main() {
    struct student *s,s1;
    s=&s1;
    scanf("%s %d",&s->name,&s->roll);
    printf("%s %d",s->name,s->roll);
    return 0;
}
//Enter the input and apply loop for multiople input data
